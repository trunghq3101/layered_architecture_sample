import 'package:flutter/material.dart';

class SuperSandboxHomePage extends StatelessWidget {
  const SuperSandboxHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
